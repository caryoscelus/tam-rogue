<?xml version="1.0" encoding="UTF-8"?>
<mod>
    <action name="fight">
        <object name="subject"/>
        <integer name="target"/>
        
        <code>
d = target
x, y = d//3-1, d%3-1
info('requested fight at {0}, {1}'.format(x, y))
victim = subject.getTile(x, y).get('monster')
if victim:
    action('hit', {'subject':subject, 'target':victim})
    _return = True
else:
    info('no victim')
    _return = False
        </code>
    </action>
</mod>
