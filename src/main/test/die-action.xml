<?xml version="1.0" encoding="UTF-8"?>
<mod>
    <action name="die">
        <object name="subject"/>
        <string name="reason"/>
        
        <code>
if not reason:
    reason = 'unknown reason'
info('{0} died because: {1}'.format(subject.attr('class'), reason))

if random() &gt; 0.5:
    # create corpse
    subject.setAttr('class', 'corpse')
else:
    subject.die()
        </code>
    </action>
</mod>
