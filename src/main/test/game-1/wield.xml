<?xml version="1.0" encoding="UTF-8"?>
<mod>
    <action name="wield">
        <object name="subject"/>
        
        <code>
tile = subject.getTile()
stack = tile.get('objects')
try:
    object = stack[-1]
    info('{0} is trying to pickup {1}'.format(subject.attr('class'), object.attr('class')))
    oldWeapon = subject.get('weapon')
    subject.remove(oldWeapon, 'weapon')
    subject.put('inv', oldWeapon)
    subject.put('weapon', object)
    tile.remove(object, 'objects')
    _return = True
except IndexError:
    info('no object to pickup here!')
    _return = False
        </code>
    </action>
</mod>
