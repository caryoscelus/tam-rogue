import logging
import socket

class Receiver:
    def __init__(self, addr = None):
        self.connect(addr)
    
    def connect(self, addr):
        self.addr = addr
    
    def listen():
        logging.debug('listening..')
        return None
